{
  "commands": [
    {
      "name": "test",
      "run": "env -u DATABASE_URL TEST_BACKEND=sqlite cargo test",
      "role": "gate",
      "timeout": 300,
      "limit": 3
    }
  ],
  "vcs": {
    "org": "Chidi09",
    "repo": "djangors"
  }
}
